server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # cache leve para o html
    location = /index.html {
        add_header Cache-Control "no-cache";
    }
}
